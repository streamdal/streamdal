apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "snitch.fullname" . }}-test-connection"
  labels:
    {{- include "snitch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "snitch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
