{
  "input": "UPDATE orders SET total = total * 0.9 FROM users WHERE users.id = orders.user_id AND users.status = 'VIP';",
  "outputs": [
    {
      "expected": "UPDATE orders SET total = total * ? FROM users WHERE users.id = orders.user_id AND users.status = ?",
      "statement_metadata": {
        "size": 17,
        "tables": [
          "orders",
          "users"
        ],
        "commands": [
          "UPDATE"
        ],
        "comments": [],
        "procedures": []
      }
    }
  ]
}