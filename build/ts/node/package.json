{
  "name": "@streamdal/snitch-protos",
  "version": "0.0.1",
  "description": "Snitch Protobufs",
  "main": "protos/external_api_client.js",
  "scripts": {
    "build": "npm run gen && npm run deno:esm && npm run node:esm && npm run clean && npm run node:compile && npm run copy",
    "node:esm": "node esm-js.js",
    "node:compile": "tsc",
    "clean": "rimraf build coverage nyc_output",
    "copy": "cp DENO.README.md ./deno/README.md && cp NODE.README.md ./node/README.md && cp package.json ./node/package.json",
    "deno:esm": "node esm-ts.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "type:dts": "tsc --emitDeclarationOnly --project tsconfig.build.json",
    "gen": "npx protoc --ts_out ./protos --ts_opt optimize_code_size --proto_path ../../protos ../../protos/**/*.proto ../../protos/*.proto"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/streamdal/snitch-protos.git"
  },
  "keywords": [
    "Snitch",
    "Protobuf",
    "Streamdal"
  ],
  "author": "Streamdal",
  "license": "MIT",
  "type": "module",
  "bugs": {
    "url": "https://github.com/streamdal/snitch-protos/issues"
  },
  "homepage": "https://github.com/streamdal/stitch-protos/build/ts/node",
  "dependencies": {
    "@grpc/grpc-js":"^1.8.18",
    "@protobuf-ts/plugin": "^2.9.0"
  },
  "devDependencies": {
    "glob": "^10.3.3",
    "rimraf": "^5.0.1",
    "typescript": "^5.1.6"
  }
}
